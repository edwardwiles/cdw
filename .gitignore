# intermediate Stata data and logs (regenerated by the pipeline)
output/intermediate/
*.dta
*.log
# python
__pycache__/
*.pyc
# raw data lives outside the repo (see config.do / paths); never commit it
data/
raw/
