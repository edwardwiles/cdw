Melitz reduced-q-subspace validation and D20 readiness -- provenance (2026-07-29 continuation session)

Repository: trade_robustness_modular
Branch (before and after): melitz/fullD-delta-star
HEAD before this session's edits: bda67c10a0d0bde34068e80f7246289a70911dc3
  ("Sequential reduced-q-subspace outer-search backend (experimental) + prior-session
  diagnostic corrections")
HEAD after this session's commit: see `git log -1` after the commit made at the end of this
  session (committed locally, NOT pushed, per governing prompt instruction).
Remote status before/after: 38 commits ahead of cdw/melitz/fullD-delta-star before this
  session's commit; +1 after. Not pushed.

Julia: 1.12.6 (juliaup, ~/.juliaup/bin)
KNITRO: 13.0.1 (pinned per .knitro_env.sh, ZIENA_LICENSE=/etc/sharedsw_licenses/ziena.txt,
  KNITRODIR=/opt/shared_sw/knitro/13.0.1)
Julia threads / BLAS threads:
  - Phase 0 audit/repair scripts, Gate 1 (D4 W-replay), Gate 2 (D4 matched-effort ablation):
    -t 1, OPENBLAS_NUM_THREADS=1, OMP_NUM_THREADS=1 (matches this codebase's own established
    convention for D4-scale diagnostic/campaign scripts with no Threads.@threads-eligible
    kernel of their own).
  - Gate 3 (D20 threading benchmark + bandwidth/stage-radius calibration): -t 20,
    OPENBLAS_NUM_THREADS=1, OMP_NUM_THREADS=1 (src/melitz/CLAUDE.md's 20-thread-by-default
    policy for D20 performance work); melitz_thread_startup_report() confirmed "Julia threads
    available = 20 (require >= 20), BLAS threads selected = 1" at run start.
  - Threads.maxthreadid() observed = 40 during Gate 3 (NOT 20 = Threads.nthreads()) -- see the
    main report doc's Gate 3A section for the live BoundsError this caused and its fix (the
    same fix already established in localized_gradient.jl, attributed not rediscovered).

Host: 208 logical CPUs, ~3.0TiB RAM, shared.

Base economies:
  - D4: sigma=2.5, theta_star=6.8, target_country=1, seed=29 (generate_fake_melitz_data),
    matching the ORIGINAL Phase 12 session's own fixture exactly, for direct comparability.
  - real D20: noah_D20 (real_data/noah_D20/{pi,L,tau,countries}.csv), sigma=2.5,
    theta_star=:estimate, focal_country="fra", seed=1 (calibrate_melitz_pareto), matching the
    prior session's own Phase 10/13 fixture exactly.

QMC seeds / W used per phase:
  - Phase 0 repair rerun: D4 seed=29, W=20,000 (matching the original Phase 12 fixture).
  - Gate 1: D4 seed=29 (economy) x W in {20000,80000,320000,1280000} (scramble seed=29, the
    SAME scramble the states were originally found under); held-out scramble seed=49 (D4's
    own known-good seed list per memory feedback-melitz-d4-seed-fragility --
    [29,49,50,52,53,94,107,110] -- seed=141 was tried first and FAILED live
    ("export-selection zhat[2,3]>=zhat[2,2] violated"), confirmed and disclosed, not hidden;
    49 substituted).
  - Gate 2: D4 seed=29, W=80,000 (common starting state derived from the Gate-1-verified
    Phase-12 incumbents).
  - Gate 3: real D20, seed=1, W=80,000 throughout (mandatory development/benchmark W per the
    governing prompt).

Starting-state fingerprints / theta_free sources:
  - Gate 1/2: read directly from
    docs/key_results/melitz_reducedq_phase12_incumbent_checkpoints_2026-07-29.jls (this
    session's own corrected Phase 12 rerun's incumbent states -- the ORIGINAL Phase 12 run's
    own incumbent states were never persisted to disk, so this is the only available source;
    disclosed explicitly in the main report doc, not silently substituted).
  - Gate 3: read directly from docs/key_results/melitz_qbw_phase3_theta_q_2026-07-29.csv,
    row ("realD20_seed1_W80000", 0.5) -- the prior session's own verified :logcutoff fixed-A/f
    gamma-profile point, not re-derived this session.

Direction fingerprints: melitz_reduced_q_stage_fingerprint (reduced_q_subspace.jl, unmodified)
  hashes ctx content fingerprint + stage_id + q_anchor_free + q_basis_free +
  string(bandwidth_policy) + W + qmc_seed -- recorded per Gate-2 stage in
  docs/key_results/melitz_gate2_d4_matched_effort_stages_2026-07-29.csv's own
  `stage_fingerprint` column.

Inner evaluation policies: CappedEvaluation(10.0) throughout every script and every test this
  session added or ran -- no FullValueEvaluation use anywhere in this session's own new code.

Exact commands run this session (representative; see scripts/ for full argument lists):
  export PATH="$HOME/.juliaup/bin:$PATH"; source .knitro_env.sh
  export OPENBLAS_NUM_THREADS=1 OMP_NUM_THREADS=1
  julia --project=. -t 1 test/melitz/runtests.jl                                              # baseline + final
  julia --project=. -t 1 scripts/melitz_phase0_diagnose_reducedq_counter_invariant_2026-07-29.jl
  julia --project=. -t 1 scripts/melitz_reducedq_phase12_d4_comparison_CORRECTED_2026-07-29.jl
  julia --project=. -t 1 scripts/melitz_gate1_d4_w_replay_2026-07-29.jl
  julia --project=. -t 1 scripts/melitz_gate2_d4_matched_effort_2026-07-29.jl
  julia --project=. -t 20 scripts/melitz_gate3_d20_readiness_2026-07-29.jl

git diff --stat (source/test/docs, this session's own edits; see `git show --stat HEAD` after
  the commit for the authoritative list):
  new file:   src/melitz/typed_eval_counters.jl
  new file:   src/melitz/matched_effort_controller.jl
  new file:   src/melitz/reduced_q_threaded_direction.jl
  new file:   docs/melitz_reduced_q_validation_and_d20_readiness_2026-07-29.md
  new file:   scripts/melitz_phase0_diagnose_reducedq_counter_invariant_2026-07-29.jl
  new file:   scripts/melitz_reducedq_phase12_d4_comparison_CORRECTED_2026-07-29.jl
  new file:   scripts/melitz_gate1_d4_w_replay_2026-07-29.jl
  new file:   scripts/melitz_gate2_d4_matched_effort_2026-07-29.jl
  new file:   scripts/melitz_gate3_d20_readiness_2026-07-29.jl
  new file:   docs/key_results/melitz_gate1_d4_w_replay_2026-07-29.csv
  new file:   docs/key_results/melitz_gate2_d4_matched_effort_stages_2026-07-29.csv
  new file:   docs/key_results/melitz_gate2_d4_matched_effort_summary_2026-07-29.csv
  new file:   docs/key_results/melitz_gate3a_d20_threading_benchmark_2026-07-29.csv
  new file:   docs/key_results/melitz_gate3b_d20_bandwidth_calibration_2026-07-29.csv
  new file:   docs/key_results/melitz_reducedq_phase12_d4_comparison_CORRECTED_2026-07-29.md
  new file:   docs/key_results/melitz_reducedq_phase12_incumbent_checkpoints_2026-07-29.jls
  new file:   docs/key_results/melitz_reducedq_validation_provenance_2026-07-29.txt (this file)
  modified:   docs/key_results/melitz_reducedq_phase12_d4_comparison_2026-07-29.csv
              (regenerated in place: standards-compliant, validated, fresh rerun -- see main
              report doc for why a rerun was necessary, not merely a formatting fix)
  modified:   src/melitz/include_melitz.jl        (3 new include lines, additive)
  modified:   test/melitz/runtests.jl             (3 new include lines + 2 new testsets,
                                                     additive)

Zero lines of src/melitz/finite_delta_outer.jl, src/melitz/reduced_q_subspace.jl,
src/melitz/reduced_q_controller.jl, src/melitz/aq_experimental_backend.jl, or any other
existing production/experimental-backend file were modified this session (confirmed via
`git diff --name-only`, not merely asserted) -- every prior session's own backend is reused
via its existing public API, never touched.

Test suite: `julia --project=. -t 1 test/melitz/runtests.jl` run eight times this session
  (plus numerous smaller isolation runs) --
  (1) baseline, BEFORE any edit: confirmed clean, 66 top-level testsets, every one
      Pass==Total, exit code 0;
  (2) two genuine bugs in this session's OWN new test code caught and fixed (wrong
      hand-picked counterexample; out-of-scope variable reference across @testset blocks) --
      see the main report doc's own "Test suite" section for the full writeup;
  (3)-(7) five further full-suite attempts, all ending in a non-deterministic-location
      SIGSEGV inside (never before) this session's own two new testsets, diagnosed at length
      as a host-level memory-pressure issue (swap 4.0/4.0 GiB utilized during this window,
      confirmed via `free -h`, alongside another session's own concurrent large job on this
      shared host) rather than a logic bug -- every implicated function/code path was
      separately verified correct via repeated, successful, fully-isolated single-process
      runs (see the main report doc's own "Test suite" section for the complete list and
      results). Two general-principle hardening fixes were applied regardless (KNITRO option
      file duplicate-key elimination; redundant live-KNITRO test calls removed), disclosed as
      NOT confirmed to be the crash's root cause.
  A single, fully clean start-to-finish run of the combined ~7,500-line suite could not be
  obtained on this host within this session's time budget -- disclosed explicitly as an open
  item for a future session (ideally run on a quieter host, or split into two invocations:
  pre-existing testsets, then this session's new ones, separately) rather than papered over.
  This does NOT put the session's own substantive findings in question: Gates 1-3 were
  produced by real, end-to-end script executions (not the unit test suite) that completed
  successfully and are reported in full in the main document.

Work committed locally at the end of this session (see `git log -1` for the exact hash) --
NOT pushed to any remote, per this repo's own standing "confirm before pushing to a real
remote" policy and this session's own governing prompt instruction.
